#include <stdio.h>
int main() 
{
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);
    if(num % 2 == 0)
        printf("%d it  is even.", num);
    else
        printf("%d  it is odd.", num);
    
    return 0;
}
